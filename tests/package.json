{
  "name": "tests",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "jest --watchAll --verbose",
    "test:cov": "jest --coverage --watchAll",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/jest": "^29.1.1",
    "jest": "^29.1.2",
    "vite": "^3.1.0"
  },
  "dependencies": {
    "axios": "^1.0.0"
  }
}
